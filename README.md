# Create Req Chat

A static prototype for a chat interface that turns a request into a visible build sequence.

## Local Preview

Open `index.html` in a browser.

## Live Site

This repo is ready to serve from GitHub Pages using the `main` branch and the repository root.
